export const metadata = {
  title: "Start a Project | Get a Free Consultation",
  description:
    "Ready to build something exceptional? Contact Fundi today for a technical audit or a custom quote on your next web project.",
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
